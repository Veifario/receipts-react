import { RootState } from "../store";

export const getSearchProducts = (state: RootState) => state;

// export const getSearchProducts = (state: RootState) => state
// export const getSearchProducts = (state: RootState) => state
